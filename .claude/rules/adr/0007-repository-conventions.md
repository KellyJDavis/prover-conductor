---
paths:
  - "pyproject.toml"
  - ".github/**"
  - "tests/**"
  - "src/**"
---
<!-- Generated by scripts/gen_adr_rules.py from docs/adr/0007-repository-conventions.md. Do not edit: change the ADR and regenerate. -->

# ADR-0007 (accepted): Repository and development conventions

Binding for the files that loaded this rule.

One Python distribution with subpackages under src/; Python 3.12+, uv with a committed lock file, ruff, pyright (strict for src/), pytest with lean and network markers excluded by default, and import-linter for architectural rules. Tasks are GitHub issues with exit commands; work lands through pull requests.

Invariants:
- INV-0007-1: Tests that need Lean carry the lean marker, tests that need network carry the network marker, and the default pytest run excludes both.
- INV-0007-2: Code under src/ passes pyright in strict mode.
- INV-0007-3: The ruff lint and format checks pass.
- INV-0007-4: The import-linter contracts pass.

Full record: `docs/adr/0007-repository-conventions.md`. Enforcement: `docs/adr/enforcement.yaml`.
