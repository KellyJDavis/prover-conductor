---
paths:
  - "src/prover_conductor/gate/**"
  - "policies/**"
---
<!-- Generated by scripts/gen_adr_rules.py from docs/adr/0011-gate-profiles.md. Do not edit: change the ADR and regenerate. -->

# ADR-0011 (proposed): Gate profiles with Mathlib style as the floor

Proposed, not yet binding. Treat it as the current direction, and ask the owner before implementing anything that answers one of its open questions.

Each repository has a gate profile. Mathlib's style linters are a floor that no profile can disable; Mathlib policy checks are optional per repository, and prover-conductor replaces bundled linters it disables with its own checks for the style parts. The build tolerates only the sorry warning on locked, statement-only declarations.

Invariants:
- INV-0011-1: The build fails on any error or warning except the sorry warning on a locked, statement-only declaration.
- INV-0011-2: No repository profile can disable a linter in the style floor.
- INV-0011-3: Disabling a bundled linter, such as the header linter, enables prover-conductor's replacement check for its style parts.
- INV-0011-4: Axioms of changed declarations and their dependents stay within propext, Classical.choice, Quot.sound and the repository's allowlist; sorryAx enters only through locked statement-only dependencies.
- INV-0011-5: The pre-elaboration scan runs before anything is elaborated and rejects new axioms outside the allowlist, sorry or admit outside statement-only nodes, unsafe, implemented_by, extern, native_decide, debug options and code-executing commands.

Open questions: linter-partition, ratchet-default.

Full record: `docs/adr/0011-gate-profiles.md`. Enforcement: `docs/adr/enforcement.yaml`.
