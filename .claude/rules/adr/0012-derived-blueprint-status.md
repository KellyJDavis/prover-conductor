---
paths:
  - "src/prover_conductor/blueprint/**"
---
<!-- Generated by scripts/gen_adr_rules.py from docs/adr/0012-derived-blueprint-status.md. Do not edit: change the ADR and regenerate. -->

# ADR-0012 (proposed): Blueprint status is derived, never claimed

Proposed, not yet binding. Treat it as the current direction, and ask the owner before implementing anything that answers one of its open questions.

The project graph joins the leanblueprint TeX, parsed with leanblueprint's own plasTeX plugin, with facts from the Lean environment at the same commit. Status comes from the join, and \leanok is written only by deterministic code after the gate passes. Helper lemmas live in a working graph and are promoted to the public blueprint only when mathematically meaningful.

Invariants:
- INV-0012-1: Only the deterministic blueprint writer sets \leanok, and only after a passing gate run.
- INV-0012-2: The drift check fails on a \leanok claim the Lean facts contradict, a \uses that misses a project-local dependency a proof uses, or a \lean{} name that does not exist.
- INV-0012-3: Working-graph helper lemmas are committed as private declarations beside their parent until promoted to the blueprint.
- INV-0012-4: Blueprint prose written by agents contains no Lean identifiers.

Open questions: parse-fidelity.

Full record: `docs/adr/0012-derived-blueprint-status.md`. Enforcement: `docs/adr/enforcement.yaml`.
