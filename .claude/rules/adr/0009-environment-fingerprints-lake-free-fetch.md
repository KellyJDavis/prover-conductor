---
paths:
  - "src/prover_conductor/envs/**"
  - "src/prover_conductor/lean/**"
  - "src/prover_conductor/search/**"
---
<!-- Generated by scripts/gen_adr_rules.py from docs/adr/0009-environment-fingerprints-lake-free-fetch.md. Do not edit: change the ADR and regenerate. -->

# ADR-0009 (proposed): Environment fingerprints and Lake-free fetch

Proposed, not yet binding. Treat it as the current direction, and ask the owner before implementing anything that answers one of its open questions.

An environment fingerprint hashes lean-toolchain, the resolved lake-manifest.json and the lakefile options that affect elaboration; each fingerprint maps to a platform-built image and to pools of Lean workers. Dependencies are fetched by platform code from the manifest, with plain git at pinned revisions, so Lake never runs while networked.

Invariants:
- INV-0009-1: The fingerprint changes exactly when the toolchain, a resolved dependency revision, or an elaboration-affecting option changes.
- INV-0009-2: Every Lean request is served by a pool pinned to the requesting worktree's fingerprint.
- INV-0009-3: Search candidates are checked in the requesting environment before a prover sees them.
- INV-0009-4: A project's toolchain and dependencies change only in an environment-change task that a human approved.

Open questions: fingerprint-lakefile-options, mathlib-artifact-source, helper-version-cost.

Full record: `docs/adr/0009-environment-fingerprints-lake-free-fetch.md`. Enforcement: `docs/adr/enforcement.yaml`.
