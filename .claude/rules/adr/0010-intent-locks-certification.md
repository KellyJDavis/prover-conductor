---
paths:
  - "src/prover_conductor/gate/**"
  - "src/prover_conductor/blueprint/**"
  - "lean/**"
---
<!-- Generated by scripts/gen_adr_rules.py from docs/adr/0010-intent-locks-certification.md. Do not edit: change the ADR and regenerate. -->

# ADR-0010 (proposed): Intent locks, obligations and certification

Proposed, not yet binding. Treat it as the current direction, and ask the owner before implementing anything that answers one of its open questions.

An intent lock covers a statement and the closure of project-local definitions, structures and instances it mentions. Every gate run checks lock hashes, and Comparator certifies locked theorems whose dependency cone is sorry-free. Locks carry obligations, such as a non-vacuity witness, that must be discharged before a node counts as done or certified. Environment upgrades migrate locks with human approval.

Invariants:
- INV-0010-1: Changing a locked statement, or any definition, structure or instance in its closure, invalidates the lock and routes it back for approval.
- INV-0010-2: The certified tier is issued only after Comparator accepts, running on platform binaries, against a published challenge.
- INV-0010-3: A node with undischarged lock obligations is neither done nor certified, and every attestation reports obligation status.
- INV-0010-4: An environment upgrade re-hashes every lock and requires approval of each changed statement.
- INV-0010-5: Lock files are committed to the target repository so its own CI can verify them.

Open questions: hash-definition, comparator-scale, witness-policy.

Full record: `docs/adr/0010-intent-locks-certification.md`. Enforcement: `docs/adr/enforcement.yaml`.
