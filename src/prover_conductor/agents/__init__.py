"""Agent runtime: the agent loop, AgentSpec loading and validation, spawn_agent and
define_agent (ADR-0015), and the prover portfolio including lemma-first decomposition (ADR-0002).

Stochastic layer: may propose patches, never writes to GitHub (ADR-0001). No implementation yet.
"""
