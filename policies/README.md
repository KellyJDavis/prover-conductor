# policies/

Data that configures behavior without code changes: autonomy presets (docs/architecture.md,
section 4), gate profiles (ADR-0011) and model routing policies (ADR-0013). Empty until those
components exist.
