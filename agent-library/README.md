# agent-library/

prover-conductor's agent specs, as data (ADR-0015): one directory per agent kind with a YAML spec
and its prompt fragments. Empty until the agent runtime exists.

Not to be confused with `.claude/agents/`, which holds Claude Code subagents for developing this
repository, or with `src/prover_conductor/agents/`, the runtime that executes these specs.
